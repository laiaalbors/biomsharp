# general settings
name: test_ReUse_s1_PRED_from_scratch_europe-small-100to25_seed33
model_type: ReUseModel
scale: 4
num_gpu: auto
#find_unused_parameters: true
manual_seed: 33

tile: # use the tile mode for limited GPU memory when testing.
  tile_size: 256 # the higher, the more utilized GPU memory and the less performance change against the full image. must be an integer multiple of the window size.
  tile_pad: 0

# dataset and data loader settings
datasets:
  test_1:
    name: europe-small-100-25m-valid
    type: PairedOpticalBiomassDataset
    dataroot_gt: /mnt/gpid08/datasets/biomassa/europe-small/patches1920x1920/2010_25m_sub/validation
    dataroot_gd: /mnt/gpid08/datasets/biomassa/europe-small/patches1920x1920/2010_sentinel_Ball_25m_sub_nonans/validation  # 2010_sentinel_B11_B8_B2_25m_sub / 2010_sentinel_25m_sub
    guide_data: sentinel2
    io_backend:
      type: disk


# network structures
network_g:
  type: ReUse
  in_chans: 10 #2/3/4/6/10
  n_filters: 24 #24/16
  dropout: 0.05

# path
path:
  pretrain_network_g: /home/usuaris/imatge/laia.albors/TASKS/GlobalBiomass/repos/biomsharp/experiments/train_ReUse_s1_PRED_from_scratch_europe-small-100to25_seed33/models/net_g_45000.pth
  strict_load_g: true

# validation settings
val:
  save_img: true
  suffix: ~
  pbar: False

  metrics:
    psnr:
      type: calculate_psnr_biomass
      crop_border: 4
      test_y_channel: false
      better: higher  # the higher, the better. Default: higher
    ssim:
      type: calculate_ssim_biomass
      crop_border: 4
      test_y_channel: false
      better: higher  # the higher, the better. Default: higher
    mse:
      type: calculate_mse
      crop_border: 4
      test_y_channel: false
      better: lower  # the lower, the better
    rmse:
      type: calculate_rmse
      crop_border: 4
      test_y_channel: false
      better: lower
    mae:
      type: calculate_mae
      crop_border: 4
      test_y_channel: false
      better: lower
